#include "GLSceneRenderer.h"
#include "GLScene.h"



void GLSceneRenderer::render(GLScene& scene)
{
	/*std::for_each(scene.objects_.begin(), scene.objects_.end(), [](GLObject& object)
		{
			object.renderer().render();
		});*/
}
